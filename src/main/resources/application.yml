spring:
  datasource:
    url: jdbc:mysql://database-h-infinite-power.cxsewiog63mk.ap-southeast-2.rds.amazonaws.com:3306/test_attendance?useSSL=false&serverTimezone=UTC
    username: admin
    password: h-infinite-power
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate # 실제 운영 환경에서는 validate 사용
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect